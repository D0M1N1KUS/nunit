# NUnit 3 Framework (generalized) #

This is a fork of Nunit 3 Framework, that has the unit test aspect removed, allowing developers to define custom logging actions on Asserts and while using Constraints.
